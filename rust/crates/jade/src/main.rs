//! Jade app shell. Placeholder while the GPUI dependency tree builds — the
//! Phase-0 spike (window + telemetry-fed heatmap at 60fps) replaces this.

fn main() {
    println!("jade: GPUI spike pending");
}
